You are redesigning the SortMySkills web app — a placement interview prep platform. 
Perform a full-site UI overhaul based on the audit below. Apply every change 
consistently across ALL pages and components.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DESIGN IDENTITY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Theme: "Technical Ink" — dark-mode-first, editorial, precise. Feels like a 
premium dev tool, not a generic SaaS dashboard.

Color palette (replace ALL current colors with these CSS variables):

  --bg-base:       #0D0D10   /* near-black page background */
  --bg-surface:    #141418   /* card/panel surface */
  --bg-elevated:   #1C1C22   /* hover states, input fills */
  --border-subtle: #2A2A33   /* default card/divider borders */
  --border-focus:  #4A4A5A   /* hover/focused borders */

  --accent:        #7C6EFF   /* primary CTA, active nav — electric indigo */
  --accent-soft:   #2D2850   /* accent tinted backgrounds */
  --accent-glow:   rgba(124, 110, 255, 0.15)  /* for subtle glows on focus */

  --easy:          #3DD68C   /* Easy difficulty — emerald */
  --medium:        #F5A623   /* Medium difficulty — amber */
  --hard:          #F0566A   /* Hard difficulty — coral-red */

  --text-primary:  #F0EFF8   /* headings */
  --text-secondary:#9B9AAD   /* body, descriptions */
  --text-muted:    #5C5B70   /* labels, timestamps, hints */

Typography:
  --font-display: 'Syne', sans-serif         /* headings, nav brand */
  --font-body:    'DM Sans', sans-serif      /* body text, labels */
  --font-mono:    'JetBrains Mono', monospace /* code, stats, counts */

Import from Google Fonts:
  @import url('https://fonts.googleapis.com/css2?family=Syne:wght@500;600;700&family=DM+Sans:wght@400;500&family=JetBrains+Mono:wght@400;500&display=swap');

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TYPOGRAPHY RULES (apply sitewide)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- ALL page titles: font-family var(--font-display), font-size 28px, 
  font-weight 600, color var(--text-primary), letter-spacing -0.02em
- ALL page subtitles/descriptions: font-family var(--font-body), 
  font-size 14px, font-weight 400, color var(--text-secondary)
- ZERO all-caps text anywhere. No text-transform: uppercase on body copy.
  Labels and nav items use sentence case only.
- Stats, counts, numbers: font-family var(--font-mono), 
  color var(--text-primary)
- Section labels (small category text above headings): 
  font-size 11px, font-weight 500, letter-spacing 0.06em, 
  color var(--accent), text-transform uppercase — ONLY for these micro-labels

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMPONENT: SIDEBAR NAV
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Background: var(--bg-base), right border 1px solid var(--border-subtle)
- Brand logo: font-family var(--font-display), font-weight 700, 
  font-size 16px, color var(--text-primary)
- Nav group label ("WORKSPACE PLATFORM"): 
  font-size 10px, letter-spacing 0.1em, color var(--text-muted), 
  text-transform uppercase, margin-bottom 8px
- Nav items: 
  font-family var(--font-body), font-size 13px, font-weight 400,
  color var(--text-secondary), padding 8px 12px, 
  border-radius 6px, display flex, align-items center, gap 10px
- Nav item HOVER: background var(--bg-elevated), color var(--text-primary)
- Nav item ACTIVE: background var(--accent-soft), color var(--accent), 
  border-left 2px solid var(--accent)
- Icons: 16px, same color as text, consistent Lucide or Tabler outline set 
  — pick ONE icon library and use it everywhere
- Rename ambiguous items:
    "WHY NO REPLY" → "Application Insights"
    Keep others as-is but apply sentence case: "Job match", "Skill parser", 
    "Jd translator", "Interview packs", "Overview", "Profile"
- Bottom of sidebar: "SortMySkills Core" branding block — 
  background var(--bg-elevated), border-radius 8px, padding 12px,
  font-size 11px, color var(--text-muted)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMPONENT: INTERVIEW PACK CARD
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Replace current cards entirely. New structure:

  <div class="pack-card">
    <div class="pack-card__header">
      <span class="pack-card__tag">{category tag e.g. "Frontend"}</span>
      <button class="pack-card__bookmark" aria-label="Bookmark pack">
        <!-- bookmark icon -->
      </button>
    </div>
    <h3 class="pack-card__title">{Role name}</h3>
    <p class="pack-card__hook">{Unique one-line differentiator — see below}</p>
    <div class="pack-card__difficulty">
      <span class="diff diff--easy">Easy 50</span>
      <span class="diff diff--medium">Medium 50</span>
      <span class="diff diff--hard">Hard 50</span>
    </div>
    <div class="pack-card__footer">
      <span class="pack-card__meta">{unique stat e.g. "2,140 attempts"}</span>
      <button class="pack-card__cta">Open pack →</button>
    </div>
  </div>

Card styles:
  background: var(--bg-surface)
  border: 1px solid var(--border-subtle)
  border-radius: 12px
  padding: 20px 20px 16px
  transition: border-color 0.2s, box-shadow 0.2s
  HOVER: border-color var(--border-focus), 
         box-shadow 0 0 0 1px var(--accent-glow)

.pack-card__title: 
  font-family var(--font-display), font-size 18px, font-weight 600,
  color var(--text-primary), margin 8px 0 6px

.pack-card__hook: 
  font-size 13px, color var(--text-secondary), 
  margin 0 0 16px, line-height 1.5
  — This MUST be unique per role. Use these:
    Frontend Engineer  → "DOM, browser APIs, and system design depth"
    Backend Engineer   → "APIs, databases, distributed systems focus"
    Data Analyst       → "SQL-heavy with statistics and case questions"
    ML Engineer        → "Math foundations + MLOps + coding rounds"
    (Add similar for any other roles)

Difficulty pills (.diff):
  font-family: var(--font-mono)
  font-size: 11px
  font-weight: 500
  padding: 3px 8px
  border-radius: 4px
  display: inline-flex, align-items center, gap 4px
  .diff--easy:   background rgba(61,214,140,0.12), color var(--easy)
  .diff--medium: background rgba(245,166,35,0.12), color var(--medium)
  .diff--hard:   background rgba(240,86,106,0.12), color var(--hard)
  — REMOVE the dot indicators. The pill IS the indicator.

.pack-card__cta (primary button):
  font-family var(--font-body), font-size 13px, font-weight 500
  padding 8px 16px, border-radius 6px
  background var(--accent), color #fff, border none
  cursor pointer
  transition background 0.15s
  HOVER: background #6A5EE8

.pack-card__meta:
  font-family var(--font-mono), font-size 12px, color var(--text-muted)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PAGE: INTERVIEW PACKS (the main grid page)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Page title: "Placement interview packs" (sentence case)
- Page subtitle: "150 questions per role, sorted by difficulty. 
  Ready for mock interview sessions."
  (No all-caps. Plain sentence case. Muted color.)
- Add a filter bar ABOVE the grid:
    [ All roles ] [ Engineering ] [ Data ] [ Design ]  
    — styled as tab pills using var(--bg-elevated) with active state 
    using var(--accent-soft) + var(--accent) text
- Grid: 2 columns on desktop, 1 on mobile. gap: 16px.
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr))

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
GLOBAL PAGE LAYOUT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- All page backgrounds: var(--bg-base)
- Content area max-width: 960px, margin 0 auto, padding 40px 32px
- Page header area (title + subtitle block): margin-bottom 32px
- Section spacing: 40px between major sections
- No decorative horizontal rules between cards — use spacing only
- Top-right settings icon: keep, style with color var(--text-muted), 
  hover color var(--text-primary)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
INTERACTION & MOTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Card hover: border + glow transition only (0.2s ease). No transform/lift.
- CTA button: background-color transition 0.15s
- Nav item hover: background transition 0.1s
- No bouncing, floating, or entrance animations on the packs grid — 
  this is a utility tool, not a marketing page. Motion should be 
  functional and subtle.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ACCESSIBILITY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- All icon-only buttons must have aria-label
- Color is never the ONLY indicator of difficulty — the text label 
  (Easy / Medium / Hard) must always be present alongside color
- Focus rings: outline 2px solid var(--accent), outline-offset 2px, 
  on all interactive elements
- Body text minimum size: 13px

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DO NOT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Do not use purple gradients or glassmorphism
- Do not use Inter or Roboto
- Do not use text-transform: uppercase on anything except the 11px 
  micro-labels described above
- Do not leave any card description as "150 curated interview questions 
  for X roles — sorted Easy, Medium, Hard." — every card needs a 
  unique hook line
- Do not use box-shadow for card depth — use border-color changes only
- Do not add decorative blobs, mesh gradients, or background patterns

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
APPLY CHANGES TO: all pages, all components, global stylesheet
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Start with: globals.css (or equivalent) → sidebar component → 
pack card component → interview packs page → then propagate to 
all other pages using the same token system.